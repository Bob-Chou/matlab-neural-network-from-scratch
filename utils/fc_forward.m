function [y, cache] = fc_forward(W, b, x)
    %======================================================
    % @ inputs:
    %   W: the multiply parameters, should be a matrix in [dimension, output] format
    %   b: the bias, should be a vector
    %   x: the data, should be a matrix in [batch, dimenson] format
    % @ returns:
    %   y: a matrix computed as x * W + b
    %   cache: cache from forward
    %=======================================================

end
